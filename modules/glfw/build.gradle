plugins {
  id 'java-library'
}

dependencies {
  api project(':fmhi-gfx')
  api project(':fmhi-mod')

  compileOnly "org.lwjgl:lwjgl:${lwjgl_version}"
  compileOnly "org.lwjgl:lwjgl-glfw:${lwjgl_version}"
}
